[project]
name = "etl-pipeline"
version = "0.1.0"
description = "ETL pipeline for FieldCommander wildfire smoke and crop-risk intelligence"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
    "rasterio>=1.4.0",
    "streamlit>=1.42.0",
]
